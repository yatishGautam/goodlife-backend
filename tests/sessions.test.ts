import { describe, it } from 'vitest';

// TODO: Implement in Phase 6
describe('Sessions', () => {
  it.todo('POST /sessions/start — creates active session');
  it.todo('PATCH /sessions/:id/log-set — appends set log');
  it.todo('PATCH /sessions/:id/complete — completes session');
  it.todo('POST /sessions — bulk upload completed session');
  it.todo('GET /sessions — paginated list');
  it.todo('GET /sessions/calendar/:year/:month');
  it.todo('DELETE /sessions/:id — soft-delete');
});
