import { describe, it } from 'vitest';

// TODO: Implement in Phase 5
describe('Templates', () => {
  it.todo('GET /templates — lists templates');
  it.todo('POST /templates — creates template with exercises');
  it.todo('PUT /templates/:id — updates template');
  it.todo('DELETE /templates/:id — soft-deletes');
  it.todo('POST /templates/:id/duplicate');
  it.todo('GET /templates/today — today suggestion logic');
});
